version: '3.9'

services:
  web:
    build: .
    ports:
      - '8000:8000'
    env_file:
      - .env
    environment:
      FLASK_ENV: production
      PYTHONUNBUFFERED: '1'
      GEMINI_API_KEY: ${GEMINI_API_KEY}
    restart: unless-stopped
